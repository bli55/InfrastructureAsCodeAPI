# This file is managed by SOPS. Do not edit manually.
# Generate it by running: sops --encrypt secrets.yaml > secrets.enc.yaml
# Prerequisites:
#   1. Run terraform apply to create the KMS key
#   2. Set kms ARN in .sops.yaml
#   3. Have AWS credentials with kms:Encrypt permission
#
# Replace this entire file with the SOPS output after running the encrypt command.
api_key: PLACEHOLDER_ENCRYPT_ME
db_password: PLACEHOLDER_ENCRYPT_ME
sops:
    kms:
    -   arn: arn:aws:kms:us-east-1:ACCOUNT_ID:key/KEY_ARN
        created_at: "1970-01-01T00:00:00Z"
        enc: PLACEHOLDER
    version: 3.9.1
